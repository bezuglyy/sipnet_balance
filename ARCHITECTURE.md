# Архитектура и эксплуатация

`config_flow.py` создаёт запись SIPNET, `sensor.py` выполняет запрос баланса, `__init__.py` регистрирует ручное обновление `sipnet_balance.refresh`. Период опроса задаётся пользователем.

Пароль хранится только в Config Entry и не должен попадать в журнал или commit.
